var health = document.getElementById("health");
health.style.width = "<%= @happiness %>%";

init = $(".points__house").text();

$(".points__house").text("<%= @points %>");
pointCounter(Number(init));

// setInterval(() => { health.style.width = `${(parseInt(health.style.width.slice(0, 2)) - 1) + '%'}` }, 100)
