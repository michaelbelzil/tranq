// Change counter
init = $(".points__house").text();
$(".points__house").text("<%= @points %>");
pointCounter(Number(init));

//Change page without upload
$(".added__items .toolbar__item--<%= @category %>").css('background-image', 'url(<%= @url %>)')

// Change navbar
var clone = $(".navbar__specific__item--<%= @category %>").last().clone().appendTo(".<%= @category %>_items")
clone.attr("id","favourite-<%= @item.id %>");
clone.find(".toolbar__item").css('background-image', 'url(<%= @url %>)');
clone.find("a").attr("href","/tranqs/<%= current_user.id %>?category=<%= @category %>&item_id=<%= @item.id %>");


logicDecorate();
